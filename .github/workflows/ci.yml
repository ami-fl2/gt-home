name: Build and Release

on:
  push:
    branches:
      - main
    tags:
      - '*'
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g., v1.0.0)'
        required: true
        type: string

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v4
        with:
          go-version: '1.25'

      - name: Run tests
        run: |
          cd go
          go mod tidy
          go test -v ./...

  integration:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v3
        with:
          version: 'latest'

      - name: Create KinD cluster
        uses: helm/kind-action@v1.7.0
        with:
          cluster_name: local

      - name: Load and install Fibonacci
        run: |
          docker pull ghcr.io/ami-fl2/fibs:1.0.2
          kind load docker-image ghcr.io/ami-fl2/fibs:1.0.2 --name local
          make helm-install

      - name: Test service endpoint
        run: |
          make port-forward
          make test-endpoint
          make health-check

      - name: Show logs on failure
        if: failure()
        run: make logs

  release:
    needs: [test, integration]
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Create tag from input
        id: create-tag
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git tag v${{ github.event.inputs.tag }}

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: '~> v2'
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REGISTRY: ghcr.io/${{ github.repository_owner }}

      - name: Push tag on success
        if: success()
        run: git push origin v${{ github.event.inputs.tag }}

      - name: Delete tag on failure
        if: failure()
        run: git tag -d v${{ github.event.inputs.tag }}